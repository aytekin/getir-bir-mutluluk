spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/getir_db
    username: postgres
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

application:
  jwt:
    secret: applicationjwtsecretkey
    expires: 360000000